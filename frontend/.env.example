# URL da API para desenvolvimento/produção
# Se o frontend for servido pelo backend, deixe como /api
# Se o frontend rodar em porta diferente, use a URL completa, ex: http://localhost:3006/api
VITE_API_URL=/api
